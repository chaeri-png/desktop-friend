# 🍎 Mac 설치 안내 (Desktop friend by chaeri)

이 앱은 애플 개발자 등록(연 $99)을 하지 않은 사내 배포용 앱이라, 맥이 "모르는 앱"으로 취급해요.
바이러스가 아니라 **애플에 등록되지 않아서 뜨는 경고**입니다.

아래 **A안**을 먼저 해보고, 안 되면 **B안**으로 하면 100% 열립니다.

---

## A안. 일반 설치 (보통 이걸로 됩니다)

1. 받은 `.dmg` 파일을 더블클릭해서 열기
2. 앱 아이콘을 **Applications(응용 프로그램)** 폴더로 드래그
3. 응용 프로그램에서 앱을 더블클릭 → **경고창이 뜨면 닫기**
4.  → **시스템 설정** → 왼쪽에서 **개인정보 보호 및 보안** 클릭
5. 화면을 아래로 쭉 내리면 **"'Desktop friend by chaeri'이(가) 차단되었습니다"** 문구와 함께
   **[그래도 열기]** 버튼이 있어요 → 클릭 → 맥 비밀번호 입력
6. 다시 한 번 물어보면 **[열기]** 클릭 → 끝! 다음부터는 그냥 더블클릭으로 실행돼요

---

## B안. 확실한 방법 (A안이 안 되거나 "휴지통으로 이동" 메시지가 뜰 때)

터미널 명령 한 줄이면 경고 자체가 사라집니다. 어렵지 않아요!

1. 휴지통에 들어간 앱이 있다면 **완전히 비우기**
2. 받은 `.dmg`를 열고 앱을 **Applications 폴더로 드래그**
3. **터미널** 실행 (Spotlight 검색: `⌘ + Space` → "터미널" 입력 → 엔터)
4. 아래 줄을 **그대로 복사해서 붙여넣고 엔터** (비밀번호를 물어보면 맥 로그인 비밀번호 입력, 화면에 안 보여도 정상)

```bash
sudo xattr -rd com.apple.quarantine "/Applications/Desktop friend by chaeri.app"
```

5. 이제 응용 프로그램에서 앱을 더블클릭 → **경고 없이 바로 실행됩니다**

> 이 명령은 "인터넷에서 받은 파일" 표시를 떼는 것뿐이에요. 앱을 수정하거나 보안을 끄지 않습니다.

---

## 그래도 안 될 때

아래 정보를 채리에게 알려주세요:

1. 정확한 경고 문구 (화면 캡처면 더 좋아요)
2.  → **이 Mac에 관하여** → **칩** 항목 (예: Apple M2 / Intel Core i5)

---

## 참고: 왜 이런 경고가 뜨나요?

맥은 애플에 등록·공증(notarization)된 앱만 아무 경고 없이 실행해요.
등록에는 연 $99의 애플 개발자 계정이 필요해서, 사내 배포용인 이 앱은 등록을 하지 않았습니다.
경고를 아예 없애고 싶다면 개발자 계정 등록이 필요해요 — 팀에서 결정되면 적용할 수 있습니다.
